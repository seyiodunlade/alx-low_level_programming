#include <stdio.h>

/**
 *main - this is the starting point for the program execution
 *
 *Return: 0 only when the program is successful
 */

int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
