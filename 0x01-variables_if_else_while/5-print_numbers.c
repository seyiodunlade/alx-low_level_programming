#include <stdio.h>
/**
 * main - prints numbers from 0 to 9
 *
 * Return: 0 when code is successful
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
